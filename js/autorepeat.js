// import { CondensedIupac, Sugar, Repeat, Reaction } from 'glycan.js';

// const LACNAC = new IupacSugar();
// LACNAC.sequence = 'Gal(b1-?)GlcNAc(b1-?)*';

// let matches = sug.match_sugar_pattern(LACNAC, Reaction.Comparator );
// if (matches.length > 0) {
//   matches[0].composition().forEach( traced => traced.original.setTag(tag_symbol) );
// }


const do_repeats = () => {};

export default do_repeats;